var pongData;
function startPong(){openGameScreen('🏓 Pong');gameCanvas.style.display='block';pongData={ball:{x:gameCanvas.width/2,y:gameCanvas.height/2,dx:5,dy:5,r:10},player:{x:15,y:gameCanvas.height/2-60,w:12,h:120,score:0},ai:{x:gameCanvas.width-27,y:gameCanvas.height/2-60,w:12,h:120,score:0},particles:[],trail:[]};currentGameRestart=startPong;gameAnimation=requestAnimationFrame(pongLoop);}
function pongLoop(){if(!gameActive)return;var c=gameCanvas,ctx=gameCtx,b=pongData.ball,p=pongData.player,a=pongData.ai;ctx.fillStyle='#0A0E27';ctx.fillRect(0,0,c.width,c.height);ctx.strokeStyle='rgba(255,255,255,0.08)';ctx.setLineDash([8,12]);ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(c.width/2,0);ctx.lineTo(c.width/2,c.height);ctx.stroke();ctx.setLineDash([]);pongData.trail.push({x:b.x,y:b.y,life:8});pongData.trail=pongData.trail.filter(function(t){t.life--;ctx.globalAlpha=t.life/8;ctx.fillStyle='#00D4FF';ctx.beginPath();ctx.arc(t.x,t.y,b.r*(t.life/8),0,Math.PI*2);ctx.fill();return t.life>0;});ctx.globalAlpha=1;b.x+=b.dx;b.y+=b.dy;if(b.y<=b.r||b.y>=c.height-b.r){b.dy*=-1;spawnPongParticles(b.x,b.y,'#fff');}if(b.x-b.r<=p.x+p.w&&b.y>=p.y&&b.y<=p.y+p.h){b.dx=Math.abs(b.dx);b.x=p.x+p.w+b.r;spawnPongParticles(b.x,b.y,'#D4AF37');}if(b.x+b.r>=a.x&&b.y>=a.y&&b.y<=a.y+a.h){b.dx=-Math.abs(b.dx);b.x=a.x-b.r;spawnPongParticles(b.x,b.y,'#FF4757');updateGameScore(1);}if(b.x<0){a.score++;b.x=c.width/2;b.y=c.height/2;b.dx=5;if(a.score>=7)endGame('😞 AI Wins! '+a.score+'-'+p.score);}if(b.x>c.width){p.score++;b.x=c.width/2;b.y=c.height/2;b.dx=-5;if(p.score>=7)endGame('🎉 You Win! '+p.score+'-'+a.score);}a.y+=(b.y-(a.y+a.h/2))*0.13;if(a.y<0)a.y=0;if(a.y+a.h>c.height)a.y=c.height-a.h;[p,a].forEach(function(pad,i){var grad=ctx.createLinearGradient(pad.x,pad.y,pad.x+pad.w,pad.y);grad.addColorStop(0,i===0?'#D4AF37':'#FF4757');grad.addColorStop(1,'rgba(255,255,255,0.3)');ctx.fillStyle=grad;ctx.shadowColor=i===0?'#D4AF37':'#FF4757';ctx.shadowBlur=15;ctx.beginPath();ctx.roundRect(pad.x,pad.y,pad.w,pad.h,6);ctx.fill();ctx.shadowBlur=0;});var bg=ctx.createRadialGradient(b.x,b.y,0,b.x,b.y,b.r*3);bg.addColorStop(0,'rgba(0,212,255,0.8)');bg.addColorStop(1,'rgba(0,212,255,0)');ctx.fillStyle=bg;ctx.beginPath();ctx.arc(b.x,b.y,b.r*3,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fill();ctx.fillStyle='rgba(255,255,255,0.2)';ctx.font='bold 40px Poppins';ctx.textAlign='center';ctx.fillText(p.score,c.width/4,55);ctx.fillText(a.score,c.width*3/4,55);pongData.particles=pongData.particles.filter(function(pt){pt.x+=pt.vx;pt.y+=pt.vy;pt.life--;ctx.globalAlpha=pt.life/12;ctx.fillStyle=pt.color;ctx.fillRect(pt.x,pt.y,3,3);return pt.life>0;});ctx.globalAlpha=1;gameAnimation=requestAnimationFrame(pongLoop);}
function spawnPongParticles(x,y,color){for(var i=0;i<8;i++)pongData.particles.push({x:x,y:y,vx:(Math.random()-0.5)*5,vy:(Math.random()-0.5)*5,life:12,color:color});}
document.addEventListener('mousemove',function(e){if(pongData&&gameCanvas){var r=gameCanvas.getBoundingClientRect();pongData.player.y=e.clientY-r.top-pongData.player.h/2;if(pongData.player.y<0)pongData.player.y=0;if(pongData.player.y+pongData.player.h>gameCanvas.height)pongData.player.y=gameCanvas.height-pongData.player.h;}});
document.addEventListener('touchmove',function(e){if(pongData&&gameCanvas&&gameActive){var r=gameCanvas.getBoundingClientRect();pongData.player.y=e.touches[0].clientY-r.top-pongData.player.h/2;if(pongData.player.y<0)pongData.player.y=0;if(pongData.player.y+pongData.player.h>gameCanvas.height)pongData.player.y=gameCanvas.height-pongData.player.h;e.preventDefault();}},{passive:false});
