var bubbleData;
function startBubble(){openGameScreen('🫧 Bubble Shooter');gameCanvas.style.display='block';gameCanvas.width=Math.min(window.innerWidth,400);gameCanvas.height=Math.min(window.innerHeight-100,550);bubbleData={bubbles:[],shooter:{x:gameCanvas.width/2,y:gameCanvas.height-30,angle:0},bullet:null,score:0,colors:['#FF4757','#2ED573','#00D4FF','#FFD700','#7C3AED','#FFA502'],cellSize:28,cols:Math.floor(gameCanvas.width/28),highScore:parseInt(localStorage.getItem('bubbleHigh')||'0')};for(var r=0;r<5;r++)for(var c=0;c<bubbleData.cols;c++){if(Math.random()<0.7)bubbleData.bubbles.push({x:c,y:r,color:bubbleData.colors[Math.floor(Math.random()*bubbleData.colors.length)]});}currentGameRestart=startBubble;gameAnimation=requestAnimationFrame(bubbleLoop);}
function bubbleLoop(){if(!gameActive)return;var c=gameCanvas,ctx=gameCtx,d=bubbleData,size=d.cellSize;var bgGrad=ctx.createLinearGradient(0,0,0,c.height);bgGrad.addColorStop(0,'#1a1a3e');bgGrad.addColorStop(1,'#0A0E27');ctx.fillStyle=bgGrad;ctx.fillRect(0,0,c.width,c.height);d.bubbles.forEach(function(b){var bx=b.x*size+size/2,by=b.y*size+size/2;var grad=ctx.createRadialGradient(bx-2,by-2,0,bx,by,size*0.45);grad.addColorStop(0,'rgba(255,255,255,0.3)');grad.addColorStop(0.5,b.color);grad.addColorStop(1,'rgba(0,0,0,0.3)');ctx.fillStyle=grad;ctx.beginPath();ctx.arc(bx,by,size*0.42,0,Math.PI*2);ctx.fill();ctx.fillStyle='rgba(255,255,255,0.2)';ctx.beginPath();ctx.arc(bx-size*0.1,by-size*0.1,size*0.1,0,Math.PI*2);ctx.fill();});if(d.bullet){d.bullet.y-=5;var bbx=d.bullet.x,by=d.bullet.y;ctx.fillStyle=d.bullet.color;ctx.shadowColor=d.bullet.color;ctx.shadowBlur=10;ctx.beginPath();ctx.arc(bbx,by,size*0.35,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0;d.bubbles.forEach(function(b){var bx=b.x*size+size/2,by=b.y*size+size/2;var dist=Math.hypot(bbx-bx,by-by);if(dist<size*0.7){b.color=null;d.bullet=null;d.score+=10;updateGameScore(d.score);}});if(d.bullet&&d.bullet.y<0)d.bullet=null;}d.bubbles=d.bubbles.filter(function(b){return b.color!==null;});if(d.bubbles.length===0){d.score+=50;for(var r=0;r<3;r++)for(var c=0;c<d.cols;c++){if(Math.random()<0.6)d.bubbles.push({x:c,y:r,color:d.colors[Math.floor(Math.random()*d.colors.length)]});}}if(d.bubbles.some(function(b){return b.y>=12;})){if(d.score>d.highScore){d.highScore=d.score;localStorage.setItem('bubbleHigh',d.highScore);}endGame('🫧 Score: '+d.score+' | Best: '+d.highScore);return;}ctx.fillStyle='#fff';ctx.font='bold 13px Poppins';ctx.textAlign='left';ctx.fillText('Score: '+d.score+' | Best: '+d.highScore,10,22);ctx.fillStyle=d.colors[2];ctx.beginPath();ctx.arc(d.shooter.x,d.shooter.y,size*0.4,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff';ctx.font='10px Poppins';ctx.textAlign='center';ctx.fillText('Tap',d.shooter.x,d.shooter.y+4);gameAnimation=requestAnimationFrame(bubbleLoop);}
document.addEventListener('click',function(e){if(!bubbleData||!gameActive)return;var d=bubbleData;var r=gameCanvas.getBoundingClientRect();d.shooter.x=e.clientX-r.left;d.bullet={x:d.shooter.x,y:d.shooter.y,color:d.colors[Math.floor(Math.random()*d.colors.length)]};});
